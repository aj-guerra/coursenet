{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.org/schemas/prerequisite_dsl.schema.json",
  "title": "Prerequisite DSL",
  "type": "object",
  "required": ["op", "args"],
  "properties": {
    "op": {"type": "string", "enum": ["AND", "OR", "ONE_OF", "N_OF", "NOT", "MIN_GRADE", "STANDING", "EXAM", "COURSE", "CATEGORY"]},
    "args": {"type": "array"}
  }
}

