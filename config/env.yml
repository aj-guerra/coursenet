# Do not commit secrets. Values here may be overridden by environment variables.
openai:
  base_url: ${OPENAI_BASE_URL:}
  api_key: ${OPENAI_API_KEY:}
  model: ${OPENAI_MODEL:gpt-4o-mini}
  temperature: ${OPENAI_TEMPERATURE:0.0}

proxies:
  http: ${HTTP_PROXY:}
  https: ${HTTPS_PROXY:}
  no_proxy: ${NO_PROXY:}

